# Implementing

Implement only the approved proposal. Run relevant project checks and verify every acceptance criterion.

Do not call `plan_complete` beside implementation or verification tools. Call it separately after all results confirm that the acceptance criteria are met.
